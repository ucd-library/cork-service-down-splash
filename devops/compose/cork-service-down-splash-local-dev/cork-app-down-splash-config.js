const pageTitle = 'UHHH OHHHH! Service Unavailable';

const headerText = 'HeaderText property';
const hideHeader = true;

const explanationText = `explanationText property`;
const hideExplanation = true;

const additionalText = `additionalText property`;

const statusCode = 503;
const retryAfter = 120;  // https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Retry-After

const belowImageContent = `belowImageContent property`;

export default {
  // pageTitle,
  headerText,
  // hideHeader,
  explanationText,
  // hideExplanation,
  // statusCode,
  // retryAfter,
  belowImageContent,
  additionalText
};